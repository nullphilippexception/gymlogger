<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GymLogger" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/medium_grey</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/gold</item>
        <item name="colorOnSecondary">@color/medium_grey</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
        <item name="buttonStyle">@style/PrimaryButton</item>
        <item name="textInputStyle">@style/PrimaryInputLayout</item>
        <item name="editTextStyle">@style/PrimaryEditText</item>
        <item name="android:windowBackground">@color/medium_grey</item>
        <item name="dividerVertical">@drawable/horizontal_line</item>
    </style>
</resources>